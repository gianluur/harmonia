# Harmonia – Backend dev/test dependencies
# Extends requirements.txt — install both:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0             # async test functions
pytest-httpx==0.32.0               # respx-style HTTP mocking for httpx
respx==0.21.1                      # declarative httpx request mocking (VCR pattern)
coverage[toml]==7.6.9
pytest-cov==6.0.0
anyio[trio]==4.7.0                 # async test backend
