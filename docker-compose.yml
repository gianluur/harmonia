services:
  navidrome:
    image: deluan/navidrome:latest
    restart: unless-stopped
    volumes:
      - ./data/library:/music:ro
      - navidrome_data:/data
    ports:
      - "4533:4533"

  harmonia-backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - ./data:/data
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      - navidrome

  harmonia-frontend:
    build: ./frontend
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    depends_on:
      - harmonia-backend

volumes:
  navidrome_data:
